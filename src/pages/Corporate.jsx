import { useEffect } from 'react';

function CorporatePage() {
    // reset scroll position
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return <></>;
}

export default CorporatePage;
