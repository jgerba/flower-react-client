import { useEffect } from 'react';

function PaymentPage() {
    // reset scroll position
    useEffect(() => {
        window.scrollTo(0, 0);
    }, []);

    return <></>;
}

export default PaymentPage;
